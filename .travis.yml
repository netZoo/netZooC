language: cpp

compiler: 
- g++

os:
- linux
- osx

before_install:
- chmod +x ./.travis/build.sh
- chmod 777 ./testing/ToyData

script:
- ./.travis/build.sh

branches:
  only:
  - devel

after_success:
  - bash <(curl -s https://codecov.io/bash)

