language: cpp

compiler: 
- g++

os:
- linux
- osx

before_install:
- chmod +x ./.travis/build.sh

before_script:
- cd netzoo-c/panda
- make

script:
- make test
- diff ToyOutput_FinalNetwork.pairs ../../tests/ToyData/ToyOutput_FinalNetwork.pairs

after_script: make clean

branches:
  only:
  - devel

after_success:
- bash <(curl -s https://codecov.io/bash)

